printMe = 10

printMe.times((number): Void => {
    p.multiplyBy(10).display()
})

findStack = ['test', 'test1', 'test2']

findStack.each((value, position): Void => {
    newString = ''
    newString.join(position, ': ', value, '\n').display()
})

