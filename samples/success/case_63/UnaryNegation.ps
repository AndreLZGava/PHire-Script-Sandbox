pkg PHireScript.Samples63

class UnaryNegation as scoped {
    Bool flag
    Int count

    # getInverted(): Bool {
        inverted = !this.flag
        return inverted
    }

    # getNegative(): Int {
        negative = -this.count
        return negative
    }
}
